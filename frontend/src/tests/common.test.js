import { getSearchParams } from "../js/common"

/* describe('get data', () => {
it('should return data when function is called', () => {
    const data = commonFn.getData()
    expect(data).toReturn(data)
})
}) */

describe('getUrlSearchParams', () => {
it('should return the value of search in url', () => {
   
})
})